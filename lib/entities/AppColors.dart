import 'dart:ui';

class AppColors {
  final Color background, revealed, flagged, hidden, main;

  AppColors(
      {required this.background,
      required this.revealed,
      required this.flagged,
      required this.hidden,
      required this.main});

  static void loadColorScheme() {
  }
}
